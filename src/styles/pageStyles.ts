import { createStyles } from '@mantine/core';

const pageStyles = createStyles((theme) => ({
	root: {
		zIndex: 1,
		margin: 0,
	},
	container: {
		maxWidth: '85vw',
		margin: 'auto',
	},
}));

export default pageStyles;
